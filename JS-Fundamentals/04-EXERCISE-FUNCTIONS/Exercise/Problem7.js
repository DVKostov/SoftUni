function NxNmatrix(num) {
   let array = [];
   for (let index = 0; index < num; index++) {
        array[index] = num;
   }
   for (let index = 0; index < num; index++) {
    console.log(array.join(' '))
}
}
NxNmatrix(3)